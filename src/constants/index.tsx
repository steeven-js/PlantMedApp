import {flex} from '@src/constants/flex';
import {fonts} from '@src/constants/fonts';
import {sizes} from '@src/constants/sizes';
import {colors} from '@src/constants/colors';

export const theme = {colors, fonts, sizes, flex};
